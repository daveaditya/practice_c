/*
 * Write a funciton that takes a pointer to the first node in a linked list as a
 * parameter and returns a pointer to the last node. NULL should be returned if the list is empty.
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
