/*
 * Write a menu driven program to create a linked list of a class of students
 * and perform the following operations:
 * a. Write out the contents of the list
 * b. Edit the details of a specified student
 * c. Count the number of students abve a specified age and weight
 *
 * Make use of the header file defined as:
 *
 * typedef struct {
 * 		char name[15];
 * 		int age;
 * 		float weight;
 * } DATA;
 *
 * struct linked_list {
 * 		DATA person;
 * 		Struct linked_list *next;
 * };
 *
 * typedef struct linked_list NODE:
 * typedef NODE *NDPTR;
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
