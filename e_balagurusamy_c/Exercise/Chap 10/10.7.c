/*
 * Add a function called nextdate to the program designed in exercise 10.4 to
 * perform the following task:
 *
 * a. Accepts two arguments, one of the structure data containinng the present date
 * and the second an integer that represents the numkber of days to be added
 * to the present date.
 *
 * b. Adds the days to the present date and returns the structure containing the next
 * date correctly.
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
