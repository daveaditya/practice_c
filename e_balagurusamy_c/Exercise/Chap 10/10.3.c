/*
 * Design a funciton update that would accept the data structure designed in 10.1
 * and increments time by one second and returns the new time. (If the increment results in
 * 60 seconds, then the second member is set to zero and the minute member
 * is incremented by one. Then, if the result is 60 minutes, the minute member is set to zero
 * and the hour is incremented by one. Finally when the hour becomes 24,
 * it is set to zero).
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
