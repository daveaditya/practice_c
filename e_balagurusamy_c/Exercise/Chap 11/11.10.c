/*
 * Write a function (using pointer parameter) that compares tweo integer arrays
 * to see whether they are identical. The function returns 1 if they are
 * identical, 0 otherwise.
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
