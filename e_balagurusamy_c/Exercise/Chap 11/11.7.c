/*
 * Write a program to read in an array of names and to sort then in alphabetical
 * order. Use sort function that receives pointers to the functions strcmp and
 * swap.sort in turn should call these functions via the pointers.
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
