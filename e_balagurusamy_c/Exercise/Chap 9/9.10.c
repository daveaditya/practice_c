/*
 * Deveop a modular interactive program using functions that reads the values of
 * three sides of a triangle and displays either its area or its perimeter
 * as per the request of the user. Given the three sides a, b and c
 * 		Perimeter = a + b + c
 * 		Area = sqrt (( s - a )*( s - b )*( s - c ))
 * 		where s = (a + b + c) / 2
 */
#include<stdio.h>

int main(int argc,char ** argv) {
	//Your code here.
	return(0);
}
